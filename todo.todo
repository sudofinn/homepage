when wishing with mouse, stars should move ( + the sphere from the beginning ( does it already))

using gsap to animate ( at least the text you see when you first enter website, maybe even all 
so they should start animating when the user sees them )

add 404 page

make own .svg image as web icon